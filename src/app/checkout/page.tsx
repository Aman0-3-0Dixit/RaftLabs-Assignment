import { CheckoutForm } from "@/components/CheckoutForm";

export default function CheckoutPage() {
  return (
    <div>
      <h1 className="mb-6 font-display text-3xl font-black uppercase tracking-tight text-rail-paper">
        Checkout
      </h1>
      <CheckoutForm />
    </div>
  );
}
